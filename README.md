# ChoreMate
Mobile app that helps roommates fairly divide and track household chores using schedules, automation, and reminders.

## Quick Health Check (Local)
- Start the API and confirm `GET /health` returns `{ "status": "ok" }`.
- Start the client and confirm the Home page shows API health as `ok`.
